<UserControl x:Name="fnpbs"
  x:Class="StylableFindFlowDocumentReader.FindControls.FindNextPreviousButtons"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:StylableFindFlowDocumentReader"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <StackPanel
    Orientation="Horizontal">
    <StackPanel.Resources>
      <Style
        TargetType="{x:Type Button}">
        <Setter Property="FrameworkElement.VerticalAlignment" Value="Center" />
        <Setter Property="FrameworkElement.HorizontalAlignment" Value="Center" />
        <Setter Property="Control.BorderThickness" Value="0" />
        <Setter Property="FrameworkElement.Width" Value="15" />
        <Setter Property="FrameworkElement.Height" Value="18" />
        <Setter Property="Control.Padding" Value="0" />
        <Setter Property="FrameworkElement.Margin" Value="0" />
        <Setter Property="Control.BorderBrush" Value="Transparent" />
        <Setter Property="Control.Template">
          <Setter.Value>
            <ControlTemplate
              TargetType="{x:Type Button}">
              <Border
                Margin="{TemplateBinding FrameworkElement.Margin}"
                Padding="{TemplateBinding Control.Padding}"
                Background="Transparent">
                <Grid
                  Width="{TemplateBinding FrameworkElement.Width}"
                  Height="{TemplateBinding FrameworkElement.Height}">
                  <Border x:Name="TrimBorder"
                    Background="Transparent"
                    BorderThickness="1">
                    <ContentPresenter x:Name="ButtonContent" />
                  </Border>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsEnabled" Value="false">
                  <Setter TargetName="ButtonContent" Property="UIElement.Opacity" Value="0.3" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </StackPanel.Resources>
    <Button
      Command="{Binding PreviousCommand}"
      FocusVisualStyle="{Binding ElementName=fnpbs, Path=FocusVisualStyle}"
      ToolTip="{Binding ElementName=fnpbs, Path=FindPreviousTooltip}"
      ToolTipService.IsEnabled="{Binding ElementName=fnpbs, Path=ShowTooltips}"
      ToolTipService.ShowOnDisabled="true">
      <Button.Content>
        <Canvas
          Width="6"
          Height="7">
          <Path
            Data="F1 M 4,0 L 4,7 -2,3.818359 "
            Fill="{Binding ElementName=fnpbs, Path=Foreground}" />
        </Canvas>
      </Button.Content>
    </Button>

    <Button
      Command="{Binding NextCommand}"
      FocusVisualStyle="{Binding ElementName=fnpbs, Path=FocusVisualStyle}"
      ToolTip="{Binding ElementName=fnpbs, Path=FindNextTooltip}"
      ToolTipService.IsEnabled="{Binding ElementName=fnpbs, Path=ShowTooltips}"
      ToolTipService.ShowOnDisabled="true">
      <Button.Content>
        <Canvas
          Width="6"
          Height="7">
          <Path
            Data="F1 M -1,0 L -1,7 5,3.818359 "
            Fill="{Binding ElementName=fnpbs, Path=Foreground}" />
        </Canvas>
      </Button.Content>
    </Button>
  </StackPanel>
</UserControl>
