<UserControl x:Class="DemoCommon.FlowDocumentReaderDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DemoCommon"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel x:Name="DockPanel"
    LastChildFill="True">
        <StackPanel
      DockPanel.Dock="Top"
      Margin="5"
      Orientation="Horizontal">
            <RadioButton x:Name="IsFindEnabledRadioButton"
        Content="Find Enabled"
        IsChecked="true" />
            <RadioButton
        Margin="5,0,0,0"
        Content="Find Disabled" />
        </StackPanel>
        <local:DocumentNullSwitcher DockPanel.Dock="Top" FlowControl="{Binding RelativeSource={RelativeSource AncestorType=local:FlowDocumentReaderDemo}, Path=FlowDocumentReader}"/>
        <ContentPresenter Content="{Binding RelativeSource={RelativeSource AncestorType=local:FlowDocumentReaderDemo}, Path=FlowDocumentReader}"/>
    </DockPanel>
</UserControl>
