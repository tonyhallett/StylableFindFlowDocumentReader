<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DemoCommon"
                    >
    <FontFamily
      x:Key="fontFamily">
        Bauhaus 93
    </FontFamily>
    <Style
      TargetType="ToolTip">
        <Setter Property="Background" Value="{Binding TooltipBackground}" />
        <Setter Property="Foreground" Value="{Binding TooltipForeground}" />
        <Setter Property="BorderBrush" Value="{Binding TooltipBorder}" />
        <Setter Property="FontFamily" Value="Bauhaus 93" />
    </Style>
    <Style
      TargetType="MenuItem">
        <Setter Property="FontFamily" Value="Bauhaus 93" />
    </Style>
    <Style
      x:Key="radioButtonFocusVisualStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
              Margin="19,0,0,0"
              SnapsToDevicePixels="true"
              Stroke="{Binding MainForeground}"
              StrokeDashArray="1 2"
              StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
      TargetType="{x:Type RadioButton}">
        <Setter Property="Control.Background" Value="Transparent" />
        <Setter Property="Control.BorderBrush" Value="{Binding MainForeground}" />
        <Setter Property="Control.Foreground" Value="{Binding MainForeground}" />
        <Setter Property="Control.BorderThickness" Value="2" />
        <Setter Property="Control.FocusVisualStyle" Value="{StaticResource radioButtonFocusVisualStyle}" />
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate
            TargetType="{x:Type RadioButton}">
                    <Grid x:Name="templateRoot"
              Background="Transparent"
              SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition
                  Width="Auto" />
                            <ColumnDefinition
                  Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="radioButtonBorder"
                Margin="1,1,2,1"
                HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                Background="{TemplateBinding Control.Background}"
                BorderBrush="{TemplateBinding Control.BorderBrush}"
                BorderThickness="{TemplateBinding Control.BorderThickness}"
                CornerRadius="100">
                            <Grid x:Name="markGrid"
                  Margin="2">
                                <Ellipse x:Name="optionMark"
                    MinWidth="6"
                    MinHeight="6"
                    Fill="{TemplateBinding Control.Foreground}"
                    Opacity="0" />
                            </Grid>
                        </Border>
                        <ContentPresenter x:Name="contentPresenter"
                Grid.Column="1"
                Margin="{TemplateBinding Control.Padding}"
                HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                Focusable="false"
                RecognizesAccessKey="true"
                SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ContentControl.HasContent" Value="true">

                            <Setter Property="Control.Padding" Value="4,-1,0,0" />
                        </Trigger>

                        <Trigger Property="ToggleButton.IsChecked" Value="true">
                            <Setter TargetName="optionMark" Property="UIElement.Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="ToggleButton.IsChecked" Value="{x:Null}">
                            <Setter TargetName="optionMark" Property="UIElement.Opacity" Value="0.56" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:DemoWindow">
        <Setter Property="DataContext">
            <Setter.Value>
                <Binding Source="{x:Static local:PaletteSwitcher.Instance}" Path="SelectedPalette"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="{Binding MainBackground}"/>
        <Setter Property="Foreground" Value="{Binding MainForeground}"/>
        <Setter Property="Width" Value="800"/>
        <Setter Property="Height" Value="450"/>
    </Style>
</ResourceDictionary>